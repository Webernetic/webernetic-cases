mixin layout(sectionClass, wrapperClass)

    section(class=sectionClass)

        .container 

            article(class=wrapperClass)

                block


mixin list(class, items)

    ul

        each val in items
        
            li= val